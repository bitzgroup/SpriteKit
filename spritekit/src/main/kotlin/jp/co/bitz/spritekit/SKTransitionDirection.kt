package jp.co.bitz.spritekit

/**
 * The direction of motion in an [SKTransition] — mirrors Apple's `SKTransitionDirection`. See
 * each [SKTransition] factory's docs for exactly what moves which way.
 */
public enum class SKTransitionDirection {
    Up,
    Down,
    Left,
    Right,
}
